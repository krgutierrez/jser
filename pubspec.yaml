name: jser
description: A starting point for Dart libraries or applications.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.7.0 <3.0.0'

dependencies:
  source_gen: ^0.9.5
  build: ^1.2.2
  analyzer: ^0.39.4
  build_runner: ^1.7.4

dev_dependencies:
  pedantic: ^1.8.0
  test: ^1.6.0
